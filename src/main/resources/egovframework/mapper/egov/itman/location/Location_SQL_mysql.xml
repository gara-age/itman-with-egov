<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="locationDAO">
    <sql id="locationCondition">
        <where>
            DEL_YN = 'N'
        AND GRO_IDX = #{searching.groIdx}
            <if test="searching.searchKeyword != null and searching.searchKeyword != ''">
            <choose>
                <when test="searching.searchCondition == 'locCode'">
                AND LOC_CODE LIKE CONCAT('%' , #{searching.searchKeyword} , '%')
                </when>
                <when test="searching.searchCondition == 'locName'">
                    AND LOC_NAME LIKE CONCAT('%', #{searching.searchKeyword}, '%')
                </when>
                <otherwise>
                    AND(
                    LOC_CODE LIKE CONCAT('%', #{searching.searchKeyword}, '%')
                    OR LOC_NAME LIKE CONCAT('%', #{searching.searchKeyword}, '%')
                    )
                </otherwise>
            </choose>
        </if>
        </where>
    </sql>
    <select id="selectLocationsByGroup" parameterType="string" resultType="locationVO">
        SELECT
            LOC_IDX AS locIdx,
            LOC_NAME AS locName,
            LOC_CODE AS locCode
        FROM
            ITM_LOCATION
        WHERE
            DEL_YN = 'N'
        AND GRO_IDX = #{groIdx}
        ORDER BY LOC_NAME
    </select>

    <select id="selectLocationList" parameterType="pagination" resultType="locationVO">
    SELECT
        LOC_IDX AS locIdx,
        LOC_NAME AS locName,
        LOC_CODE AS locCode
    FROM ITM_LOCATION
        <include refid="locationCondition"/>
    ORDER BY LOC_NAME
    LIMIT #{startList} , #{listSize}
    </select>

    <select id="selectLocation" parameterType="locationVO" resultType="locationVO">
        SELECT
            LOC_IDX AS locIdx,
            LOC_NAME AS locName,
            LOC_CODE AS locCode,
            SL_NOTE AS slNote
            FROM
                ITM_LOCATION
        WHERE
            DEL_YN = 'N'
          AND LOC_IDX = #{locIdx}
    </select>

    <select id="selectLocationListCnt" parameterType="pagination" resultType="int">
        SELECT
            COUNT(*)
        FROM ITM_LOCATION
        <include refid="locationCondition"/>
    </select>

    <insert id="insertAssetLocation" parameterType="locationVO">
        INSERT INTO
        ITM_LOCATION
        <trim prefix="(" suffix=")" suffixOverrides=",">
            GRO_IDX,
            LOC_CODE,
            LOC_NAME,
            REG_DATE,
            REG_IDX,
            <if test="slNote != null and slNote.trim().length() > 0">SL_NOTE,</if>
        </trim>
            VALUES
                <trim prefix="(" suffix=")" suffixOverrides=",">
                    #{groIdx},
                #{locCode},
                #{locName},
                NOW(),
                #{regIdx},
        <if test="slNote != null and slNote.trim().length() > 0 ">#{slNote},</if>
                </trim>
    </insert>

    <update id="updateAssetLocation" parameterType="locationVO">
        UPDATE
        ITM_LOCATION
        SET
<trim suffixOverrides=",">
    LOC_NAME = #{locName},
    LOC_CODE = #{locCode},
    MOD_DATE = NOW(),
    MOD_IDX = #{modIdx},
    <if test="slNote != null and slNote.trim().length() > 0">SL_NOTE = #{slNote}</if>
</trim>
        WHERE
            LOC_IDX = #{locIdx}
    </update>


    <update id="deleteAssetLocation" parameterType="locationVO">
        UPDATE
        ITM_LOCATION
        SET
            <trim suffixOverrides=",">
                DEL_YN = 'Y',
                DEL_DATE = NOW(),
                DEL_IDX = #{delIdx},
                <if test="slNote != null and slNote.trim().length() > 0">SL_NOTE = #{slNote}</if>
            </trim>
        WHERE
            LOC_IDX = #{locIdx}
    </update>

</mapper>